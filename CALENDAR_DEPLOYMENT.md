# Календарь расходов - Инструкции по развертыванию

## Обзор
Добавлен новый функционал календаря расходов для планирования и отслеживания предстоящих платежей (аренда, коммунальные услуги и т.д.).

## Новые компоненты

### 1. База данных
- **Новая таблица**: `calendar_events`
- **Новый enum**: `recurrence_type` (none, daily, weekly, monthly, yearly)
- **Связи**: с таблицами `users` и `categories`

### 2. API Endpoints
- `GET /api/calendar` - получение событий с фильтрами
- `POST /api/calendar` - создание нового события
- `PUT /api/calendar/[id]` - обновление события
- `DELETE /api/calendar/[id]` - удаление события
- `PUT /api/calendar/[id]/status` - изменение статуса выполнения

### 3. Frontend компоненты
- **CalendarComponent.vue** - основной календарь с месячным видом
- **CalendarEventModal.vue** - модальное окно для добавления/редактирования событий
- **useCalendarStore.ts** - Pinia store для управления состоянием
- **pages/calendar.vue** - страница календаря

## Инструкции по развертыванию

### 1. Применение миграции базы данных

```bash
# Сгенерировать миграцию
npm run prisma:migrate

# Применить миграцию в продакшене
npm run prisma:deploy

# Перегенерировать Prisma клиент
npm run prisma:generate
```

### 2. Перезапуск приложения
После применения миграции перезапустите приложение:

```bash
npm run dev
```

## Функциональность

### Основные возможности:
1. **Планирование расходов** - добавление событий с датой, суммой и категорией
2. **Повторяющиеся события** - поддержка ежедневных, еженедельных, ежемесячных и ежегодных повторений
3. **Отслеживание статуса** - отметка событий как выполненных/невыполненных
4. **Календарный вид** - месячный календарь с отображением событий
5. **Фильтрация** - по категориям и статусу выполнения
6. **Статистика** - общее количество событий, выполненных и просроченных

### Цветовая индикация:
- 🔵 **Синий** - предстоящие события
- 🟢 **Зеленый** - выполненные события  
- 🔴 **Красный** - просроченные события

### Навигация:
Календарь добавлен в основное меню приложения между "Цели" и "Отчеты".

## Примеры использования

### Типичные события:
- Оплата аренды квартиры (ежемесячно)
- Коммунальные услуги (ежемесячно)
- Страховые платежи (ежегодно)
- Подписки на сервисы (ежемесячно)
- Налоговые платежи (ежегодно)

### Рабочий процесс:
1. Пользователь добавляет событие через кнопку "Добавить событие"
2. Выбирает категорию расхода, дату, сумму и периодичность
3. Событие отображается в календаре
4. При выполнении платежа отмечает событие как выполненное
5. Просроченные события подсвечиваются красным цветом

## Технические детали

### Схема таблицы calendar_events:
```sql
- id: UUID (первичный ключ)
- user_id: UUID (внешний ключ на users)
- category_id: UUID (внешний ключ на categories)
- title: String (название события)
- description: String? (описание, опционально)
- amount: Decimal(14,2) (сумма)
- event_date: Date (дата события)
- recurrence: recurrence_type? (тип повторения)
- is_completed: Boolean (статус выполнения)
- created_at: Timestamp
- updated_at: Timestamp
```

### Индексы:
- `idx_calendar_events_user_date` - для быстрого поиска по пользователю и дате
- `idx_calendar_events_user_completed` - для фильтрации по статусу выполнения

## Совместимость
Все изменения обратно совместимы. Существующие данные не затрагиваются.
